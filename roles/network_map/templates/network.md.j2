# Network Topology

Generated by Ansible on {{ ansible_date_time.date }}

```mermaid
graph TD

{% for group in groups %}
  {% if group != "all" and group != "ungrouped" %}
    subgraph {{ group }}
    {% for host in groups[group] %}
      {{ host }}["{{ host }}"]
    {% endfor %}
    end
  {% endif %}
{% endfor %}

{% for group in groups %}
  {% if group != "all" and group != "ungrouped" %}
    {% for host in groups[group] %}
      MikroTik --> {{ host }}
    {% endfor %}
  {% endif %}
{% endfor %}

---

# 🧪 5. Test playbook

### `playbooks/network_map.yml`

```yaml
- name: Generate network map
  hosts: localhost
  connection: local

  roles:
    - network_map

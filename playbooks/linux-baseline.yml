---
- name: Linux baseline configuration
  hosts: linux
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install common tools
      apt:
        name:
          - curl
          - git
          - htop
        state: present
